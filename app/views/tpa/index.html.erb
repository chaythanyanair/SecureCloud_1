<!DOCTYPE html>
<html>
<head>
    <title><%= @user.first_name.upcase %></title>
    <link rel="stylesheet" href="bootstrap.css">
    <link rel="stylesheet" href="users.scss">
</head>
<body>
<%=render 'tpa'%>
<br>
<br>

<div class="col-md-5">
<table class = "table table-bordered table-striped" width="0%">
  <thead>
    <tr>
      <th>Status Code</th>
      <th>File Id</th>
      <th>Hash value</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @req.each do |req| %>
      <tr>
        <td><%= req.status_code %></td>
        <td><%= req.file_upload_id %></td>
        <td><%= req.file_hash %></td>
        <td><%= link_to 'Delete', tpa_path(:id => req.id), method: :delete, class: "btn btn-danger", data: {confirm: 'Are you sure?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</body>